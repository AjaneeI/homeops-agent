[project]
name = "homeops-agent"
version = "0.1.0"
description = "Good Night Check smart-home operations agent for the Agents for Humans Hackathon."
requires-python = ">=3.11"
dependencies = [
  "strands-agents>=1.55.1",
  "strands-agents-tools>=0.8.8",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
